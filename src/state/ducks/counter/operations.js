import actions from './actions';

export const incrementCounter = actions.counter.incrementCounter;
export const decrementCounter = actions.counter.decrementCounter;

export default {
  incrementCounter,
  decrementCounter,
};